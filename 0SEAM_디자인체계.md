# 0:SEAM 디자인 체계

- 기준 원본: `images/0_SEAM_외국인_직원_정착_내비게이션/`의 20개 UI 콘셉트 이미지
- 작성일: 2026-08-14
- 적용 범위: 온보딩, 인증, 생활 조건, 홈 타임라인, 유심 안내, 은행 안내·추천·경험 공유, 개인정보 경고, 오류·성공 상태, 프로필 수정
- 문서 성격: 시각 콘셉트를 실제 화면과 컴포넌트로 옮기기 위한 제품 디자인 기준

## 1. 디자인 방향

### 1.1 제품 인상

0:SEAM은 외국인 직원이 대전 생활 정착 업무를 순서대로 처리하도록 돕는 안내형 서비스다. 화면은 장식보다 이해와 실행을 우선하며, 다음 인상을 일관되게 전달한다.

- 차분함: 흰 바탕과 짙은 회색 텍스트를 중심으로 불필요한 시각 자극을 줄인다.
- 신뢰감: 얇은 테두리, 충분한 여백, 공식 안내와 확인 시각을 명확히 표시한다.
- 실행 가능성: 한 화면에서 다음 행동이 무엇인지 알 수 있도록 주요 버튼을 한 개로 선명하게 둔다.
- 배려: 한국어를 읽는 속도가 다를 수 있는 사용자를 위해 짧은 문장, 단계 번호, 반복되는 정보 구조를 사용한다.
- 개인정보 최소화: 개인정보 수집 목적과 공유 제한을 일반 콘텐츠보다 눈에 띄게 설명한다.

### 1.2 시각 언어

- 기본 팔레트는 흰색, 거의 검정에 가까운 짙은 회색, 중간 회색, 연한 회색이다.
- 색상보다 굵기, 순서, 테두리, 상태 문구로 위계를 만든다.
- 카드와 입력창은 둥근 모서리를 사용하지만 과도하게 둥글거나 떠 있는 마케팅 카드처럼 보이지 않게 한다.
- 사진, 일러스트, 복잡한 장식은 기본 요소가 아니다. 필요한 경우에도 절차 이해를 방해하지 않는 범위에서만 사용한다.
- 경고는 노란색 아이콘과 짧은 제목으로만 강조하고, 오류는 빨간색 X 또는 명시적인 실패 문구로 구분한다.

## 2. 레이아웃 원칙

### 2.1 기준 프레임

- 모바일 우선 단일 컬럼이다.
- 화면 콘텐츠의 좌우 기본 여백은 32~50px 범위로 유지한다. 구현 시 고정값보다 `clamp()` 또는 컨테이너 패딩을 사용한다.
- 본문 최대 폭은 모바일 카드 화면 기준 약 680~700px이며, 넓은 화면에서는 중앙에 놓는다.
- 화면 상단에는 64~96px 높이의 헤더 영역을 둔다. 제목은 가운데 정렬하고, 뒤로 가기 버튼은 왼쪽에 둔다.
- 하단 탭이 있는 화면은 콘텐츠가 탭 바에 가리지 않도록 하단 안전 여백을 확보한다.
- 긴 안내 화면은 카드 안에 카드를 중첩하지 않고, 페이지 흐름 안에 독립 섹션을 세로로 배치한다.

### 2.2 간격 스케일

기본 간격 단위는 4px이며, 실제 화면에서 자주 쓰는 값은 다음과 같다.

| 토큰       |   값 | 용도                              |
| ---------- | ---: | --------------------------------- |
| `space-1`  |  4px | 아이콘과 짧은 텍스트 사이         |
| `space-2`  |  8px | 라벨과 보조 문구 사이             |
| `space-3`  | 12px | 카드 내부 작은 그룹               |
| `space-4`  | 16px | 입력 필드 내부, 일반 요소 간격    |
| `space-5`  | 20px | 카드 내부 기본 패딩               |
| `space-6`  | 24px | 섹션 내부 간격                    |
| `space-8`  | 32px | 섹션 사이                         |
| `space-10` | 40px | 큰 제목 이후, 화면 주요 블록 사이 |
| `space-12` | 48px | 온보딩의 큰 전환 영역             |

### 2.3 정렬

- 본문 제목, 설명, 폼 라벨, 카드 내용은 왼쪽 정렬한다.
- 화면 제목만 헤더에서 가운데 정렬한다.
- 금액·날짜·추천 순위처럼 비교가 필요한 값은 같은 열에 맞춘다.
- 버튼은 전체 폭 또는 콘텐츠 폭 중 행동의 중요도에 맞춰 선택한다. 주요 제출 버튼은 콘텐츠 영역 전체 폭을 우선한다.

## 3. 디자인 토큰

### 3.1 색상

이미지 기준의 색상은 강한 색상 대비가 아니라 명도 대비를 사용한다. 실제 색상값은 구현 환경에서 조정할 수 있지만 역할은 고정한다.

| 역할                   | 기준값    | 사용처                           |
| ---------------------- | --------- | -------------------------------- |
| `color.canvas`         | `#FFFFFF` | 화면 배경, 카드 배경             |
| `color.text`           | `#303030` | 제목, 주요 본문, 주요 버튼       |
| `color.text-secondary` | `#666666` | 일반 본문, 라벨                  |
| `color.text-muted`     | `#9A9A9A` | 설명, 보조 정보, 비활성 상태     |
| `color.line`           | `#D4D4D4` | 카드, 입력창, 구분선 테두리      |
| `color.line-soft`      | `#E8E8E8` | 언어 칩, 비활성 컨트롤           |
| `color.fill-soft`      | `#F5F5F5` | 아바타, 비활성 아이콘, 보조 영역 |
| `color.action`         | `#303030` | 주요 CTA, 선택된 탭              |
| `color.action-text`    | `#FFFFFF` | 주요 CTA 내부 텍스트             |
| `color.warning`        | `#F4C400` | 주의 아이콘, 임박 경고 표식      |
| `color.error`          | `#E11D1D` | 저장·전송 실패 아이콘 및 오류    |

원색 액센트, 보라색 그라디언트, 강한 그림자, 색상만으로 상태를 구분하는 표현은 사용하지 않는다.

### 3.2 타이포그래피

- 기본 글꼴: Pretendard. 한국어와 라틴 문자, 중국어·일본어가 함께 표시될 때도 줄바꿈과 숫자 가독성을 우선한다.
- 기본 글자색은 `color.text`, 설명은 `color.text-muted`를 사용한다.
- 자간은 기본값을 유지한다. 제목에 과도한 음수 자간을 적용하지 않는다.

| 스타일       |    크기 |    굵기 | 용도                            |
| ------------ | ------: | ------: | ------------------------------- |
| `display`    | 28~32px |     700 | 온보딩 브랜드, 핵심 성공 메시지 |
| `title`      | 24~28px |     700 | 페이지 주요 제목                |
| `section`    | 20~24px |     700 | 섹션 제목, 카드 그룹 제목       |
| `card-title` | 18~20px | 600~700 | 카드 제목, 단계 제목            |
| `body`       | 16~18px | 400~500 | 기본 안내 문장                  |
| `label`      | 15~17px | 500~600 | 폼 라벨, 정보 항목명            |
| `caption`    | 13~15px | 400~500 | 출처, 확인 시각, 보조 설명      |
| `button`     | 17~20px | 600~700 | 주요·보조 버튼                  |

긴 문장은 본문 행간을 1.6~1.8로 두고, 제목은 1.25~1.4로 조인다. 다국어 전환 시 텍스트가 길어져도 버튼과 카드의 높이가 급격히 변하지 않도록 줄바꿈을 허용한다.

### 3.3 형태와 그림자

| 요소             | 기준                                    |
| ---------------- | --------------------------------------- |
| 카드 모서리      | 10~14px                                 |
| 입력창 모서리    | 10~12px                                 |
| 주요 버튼 모서리 | 10~12px                                 |
| 언어 칩 모서리   | 완전한 pill, 높이 40~48px               |
| 카드 테두리      | 1.5~2px, `color.line`                   |
| 주요 버튼 그림자 | 아주 약한 하단 그림자만 허용            |
| 카드 그림자      | 기본적으로 없음, 필요 시 0 2px 6px 수준 |
| 포커스 링        | 2px 이상, 테두리와 분리된 명도 대비     |

## 4. 컴포넌트 체계

### 4.1 헤더

- 제목은 화면의 목적을 그대로 쓴다. 예: `홈 타임라인`, `은행 지점 추천`, `프로필·조건 수정`.
- 내부 화면에는 왼쪽 뒤로 가기 버튼을 둔다.
- 뒤로 가기 버튼은 아이콘만 사용하되 터치 영역은 최소 44×44px로 확보한다.
- 현재 위치를 헤더 제목만으로 이해할 수 있어야 한다. 로고나 장식 문구를 헤더에 반복하지 않는다.

### 4.2 주요 버튼

- 한 화면의 가장 중요한 다음 행동에만 짙은 회색 채움 버튼을 사용한다.
- 높이는 최소 48px, 권장 52~56px이다.
- 버튼 문구는 동작 중심으로 쓴다. `조건 저장하기`, `경험 제출하기`, `공식 사이트 방문`처럼 결과가 명확해야 한다.
- 비활성 버튼은 회색 채움과 낮은 대비로 표현하되, 비활성 이유를 주변 문구로 설명한다.
- 실패 화면의 `다시 시도`는 주요 버튼, `이전 화면으로`는 보조 버튼으로 둔다.

### 4.3 보조 버튼과 텍스트 링크

- 보조 버튼은 흰색 배경, 회색 테두리, 짙은 텍스트로 구성한다.
- `상세 보기`, `공식 안내 확인`, `홈으로 돌아가기`처럼 되돌리기·참조 행동은 밑줄 텍스트 링크로 표현할 수 있다.
- 버튼과 링크를 한 줄에 배치할 때 긴 번역문이 겹치지 않도록 flex-wrap 또는 세로 전환을 지원한다.

### 4.4 카드

카드는 정보를 읽고 다음 행동을 선택하는 단위다. 모든 정보를 카드로 감싸지 말고, 반복 항목·경고·출처·결과처럼 경계가 필요한 정보에만 사용한다.

카드 내부 순서는 기본적으로 다음을 따른다.

1. 유형 또는 상태 라벨
2. 핵심 제목
3. 짧은 설명 또는 근거
4. 필요한 날짜·출처·현장 정보
5. 카드의 단일 행동

카드에는 한 개의 핵심 행동만 둔다. 여러 행동이 필요하면 우선순위가 높은 행동을 채움 버튼으로, 나머지를 링크 또는 보조 버튼으로 둔다.

### 4.5 상태 표시

| 상태        | 표현                                          |
| ----------- | --------------------------------------------- |
| 완료        | `완료` 라벨, 낮은 대비의 보조 표현, 완료 체크 |
| 진행 중     | `진행 중` 라벨, 주요 행동 버튼 활성화         |
| 대기        | `대기` 라벨, 안내 문구와 보조 버튼            |
| 임박 경고   | 노란색 경고 아이콘 + 남은 기간 + 해야 할 일   |
| 오류        | 실패 제목 + 원인 + 재시도 + 이전 화면         |
| 성공        | 체크 아이콘 + 결과 요약 + 다음 단계           |
| 비공개·차단 | 이유를 먼저 설명하고 수정·확인 행동 제공      |

색상만으로 상태를 전달하지 않는다. 상태명, 아이콘, 문장 중 최소 두 가지를 함께 사용한다.

### 4.6 폼 컨트롤

- 라벨은 입력창 위에 놓고, placeholder를 라벨 대신 사용하지 않는다.
- 입력창 높이는 최소 48px, 모바일에서는 52~56px를 권장한다.
- 선택창은 현재 값과 펼침 아이콘을 함께 표시한다.
- 체크박스는 선택 상태가 명확한 짙은 회색 채움과 흰색 체크로 표시한다.
- 토글은 단순 선호 설정이나 완료 여부에 사용한다. 완료 상태는 텍스트 `완료`와 함께 보여준다.
- 날짜 입력은 사용자가 날짜 형식을 추측하지 않도록 날짜 선택 컨트롤을 우선한다.
- 필수 누락, 형식 오류, 개인정보 감지 오류는 입력과 가까운 곳에 즉시 표시한다.

### 4.7 하단 탭

하단 탭은 로그인 후 주요 업무 영역을 빠르게 이동하는 전역 내비게이션이다.

- 순서는 `홈`, `유심`, `은행`, `내 정보`로 고정한다.
- 아이콘과 텍스트를 함께 사용한다.
- 선택 탭은 짙은 회색 아이콘·텍스트와 상단 표시선 또는 강조 형태로 구분한다.
- 비선택 탭은 연한 회색으로 낮추되 텍스트를 읽을 수 있는 대비를 유지한다.
- 탭 바 높이와 safe-area를 포함해 콘텐츠를 가리지 않도록 한다.

## 5. 화면별 적용 규칙

### 5.1 시작·인증

대상 화면: `01` 시작 온보딩, `02` 회원가입, `03` 로그인.

- 시작 화면은 로고 영역, 서비스 한 줄 설명, 지원 언어, 개인정보 최소 수집 안내, 회원가입 CTA, 로그인 링크 순서다.
- 지원 언어는 `English`, `中文`, `Tiếng Việt`, `日本語` 칩으로 제공한다.
- 가입·로그인은 입력창을 세로로 쌓고 제출 버튼을 폼 아래에 고정한다.
- 비밀번호 규칙은 필드 주변의 보조 문구로 설명한다.
- 인증 화면은 장식보다 현재 해야 할 입력과 다음 행동을 먼저 보여준다.

### 5.2 생활 조건 입력·수정

대상 화면: `04` 생활 조건 입력, `20` 프로필·조건 수정.

- 입력 정보가 왜 필요한지 화면 상단에서 먼저 설명한다.
- 비자, 입국일, 근무지, 거주 예정지, 등록증 유효기간을 그룹으로 묶는다.
- 개인정보 수집·이용 안내는 별도 테두리 카드로 제공하고, 저장 버튼 전에 배치한다.
- 보유 서류는 체크박스 목록으로 제공한다.
- 저장 실패 링크는 주요 버튼과 분리해 오류 안내 화면으로 이동시킨다.

### 5.3 홈 타임라인

대상 화면: `06` 홈 타임라인, `07` 등록증 만료 경고, `08` 공식 안내 외부 링크, `09` 알림 전송 요청, `10` 전송 결과, `11` 전송 실패.

- 인사말과 사용자 식별자는 상단에 두되, 업무 카드보다 시각적 우선순위를 높이지 않는다.
- 임박한 체류 자격 경고는 타임라인보다 위에 노출한다.
- 타임라인 카드는 `단계·상태`, 업무명, 짧은 설명, 행동 순서로 구성한다.
- 현재 진행 중인 업무만 짙은 주요 버튼을 제공하고, 완료·대기 업무는 상세·안내 링크로 낮춘다.
- 공식 링크 화면은 외부 기관 정보임을 표시하고 확인 시각과 최신성 주의를 포함한다.
- HR 알림은 수신 대상, 전송 요약, 요청 사유, 긴급도, 공식 안내, 개인정보 주의 순으로 요약한다.
- 전송 결과는 성공·실패를 첫 화면에서 분명히 말하고, 재시도와 홈 이동 경로를 제공한다.

### 5.4 유심 절차

대상 화면: `12` 선불 유심 절차 카드, `13` 유심 공식 안내 링크.

- `준비물`, `단계별 절차`, `현장 팁`, `연락처 확보 완료`, `공식 안내 최신 확인` 순서를 유지한다.
- 단계 카드에는 큰 숫자, 단계명, 설명, 완료 상태를 반복 배치한다.
- 현장 팁은 실제 실패를 줄이는 짧은 문장으로 작성한다. 예: 원본 여권 필요 여부, 데이터 전용과 통화 가능 유심 구분.
- 완료 토글은 정보 업데이트의 결과를 설명하는 문구와 함께 둔다.

### 5.5 은행 추천·경험

대상 화면: `14` 은행 지점 추천, `15` 지점 현장 경험, `16` 은행 공식 안내 링크, `17` 은행 경험 공유.

- 추천 조건은 칩으로 먼저 요약하고, 추천 결과는 순위·지점명·주소·추천 근거·준비 서류·현장 경험·확인 시각 순으로 보여준다.
- 추천 결과가 계좌 개설을 보장하지 않는다는 고지를 목록 하단에 반복한다.
- 지점 경험 카드에는 공식 정보와 사용자 경험을 별도 섹션으로 나눈다.
- 경험 공유 폼은 개인정보를 입력하지 않도록 안내하고, 제출 전 체크박스 동의를 요구한다.
- 실제 요구 서류와 현장 경험은 공식 요건이 아니라 참고 정보라는 관계를 명확히 표시한다.

### 5.6 개인정보 경고

대상 화면: `18` 개인정보 경고, `19` 경험 제출 완료.

- 감지된 정보 유형을 계좌번호, 외국인등록번호, 연락처처럼 구체적으로 보여준다.
- 왜 공유할 수 없는지와 삭제 방법을 별도 섹션으로 설명한다.
- 수정 후 재제출 버튼은 화면 하단의 단일 주요 행동으로 둔다.
- 제출 완료 화면은 저장된 정보와 저장되지 않은 민감정보를 구분해 안심과 통제감을 제공한다.

## 6. 반응형·접근성 기준

- 최소 지원 폭은 320px이다. 320~390px에서 긴 언어명, 버튼 문구, 날짜가 잘리지 않아야 한다.
- 44×44px보다 작은 터치 대상은 만들지 않는다.
- 본문과 버튼 텍스트는 시스템 글자 확대가 적용되어도 겹치지 않아야 한다.
- 명도 대비는 일반 본문 WCAG AA를 목표로 하며, 연한 회색 텍스트를 장식이 아닌 정보에 사용할 때 대비를 재검토한다.
- 모든 아이콘 버튼에는 accessible name을 제공한다.
- 입력 오류는 색상만 사용하지 않고 오류 문장과 `aria-describedby` 연결을 제공한다.
- 키보드 포커스 순서는 화면의 읽기 순서와 같아야 한다.
- `prefers-reduced-motion`에서는 페이지 진입과 상태 전환 애니메이션을 줄인다.
- 외부 링크는 새 환경으로 이동한다는 사실을 링크 문구 또는 접근성 라벨로 알린다.

## 7. 콘텐츠 작성 규칙

- 제목은 한 화면의 목적을 직접 말한다.
- 설명은 한 문장에 한 정보만 담고, 한국어 문장은 짧게 나눈다.
- 사용자가 바로 행동해야 하는 경우 문장 끝을 `~하세요`, `~확인하세요`처럼 행동형으로 쓴다.
- 날짜·기간·기관명·문서명은 굵기나 줄바꿈으로 읽기 쉽게 분리한다.
- 번역문은 한국어의 직역보다 동일한 행동 의미와 정보 순서를 우선한다.
- 공식 정보와 사용자 경험 정보는 라벨을 분리한다.
- 개인정보 관련 문구는 수집 목적, 사용 범위, 보관 기간, 공유 제한을 빠뜨리지 않는다.
- 운영 정보가 바뀔 수 있는 화면에는 `최신 확인` 날짜와 공식 출처 링크를 함께 표시한다.

## 8. 구현 체크리스트

### 시각

- [ ] 화면 배경은 밝고, 주요 액션은 짙은 회색 하나로 명확한가?
- [ ] 카드·입력창·버튼의 테두리와 모서리가 공통 토큰을 따르는가?
- [ ] 제목, 본문, 보조 문구의 위계가 굵기·크기·색상으로 구분되는가?
- [ ] 섹션 사이 여백이 충분하고 카드가 서로 붙어 보이지 않는가?

### 상호작용

- [ ] 모든 화면에 다음 행동 또는 돌아갈 경로가 있는가?
- [ ] 완료·진행·대기·오류 상태가 문구로도 구분되는가?
- [ ] 저장·전송 실패 시 원인, 재시도, 이전 화면 경로가 있는가?
- [ ] 외부 정보에 출처와 최신 확인 시각이 있는가?

### 개인정보·신뢰

- [ ] 조건 정보가 왜 필요한지 입력 전에 설명했는가?
- [ ] 경험 공유 전에 민감정보 차단 기준을 안내했는가?
- [ ] 공식 요건과 사용자 경험을 혼동하지 않도록 라벨을 분리했는가?
- [ ] 계좌번호, 등록번호, 연락처가 화면에 불필요하게 노출되지 않는가?

### 다국어·접근성

- [ ] 네 가지 지원 언어에서 버튼과 칩이 잘리지 않는가?
- [ ] 320px 폭과 글자 확대 상태에서 레이아웃이 유지되는가?
- [ ] 터치 영역이 충분하고 키보드 포커스가 보이는가?
- [ ] 오류와 상태가 색상 외 수단으로도 전달되는가?

## 9. 현재 프론트엔드와의 정합성 메모

현재 `frontend/src/index.css`와 일부 컴포넌트는 이미지 콘셉트보다 앱형 대시보드에 가까운 파란색 강조(`app-accent`)와 상태별 색상 토큰을 사용한다. 구현을 이미지 콘셉트에 맞출 때는 다음을 우선 검토한다.

1. 주요 CTA와 선택 상태의 파란색을 짙은 회색 기반 액션 토큰으로 조정한다.
2. 이미지의 카드 밀도와 큰 세로 간격에 맞춰 페이지 패딩, 카드 모서리, 버튼 높이를 재조정한다.
3. 이미지에는 없는 과한 그라디언트와 상태별 색상 장식을 줄이고, 경고·오류에만 제한적으로 색상을 남긴다.
4. 현재 기능 정의서의 오프라인, 출처 최신성, 개인정보 차단 규칙이 화면 상태와 문구에 드러나도록 유지한다.

이 문서는 색상값 자체보다 역할과 계층을 우선하는 기준 문서다. 실제 구현에서는 반응형 테스트와 다국어 문자열 길이 테스트를 거쳐 토큰의 세부값을 확정한다.
